CREATE TABLE EMPLOYEES (
    EMPLOYEE_ID NUMBER(6) PRIMARY KEY,
    FIRST_NAME VARCHAR2(20),
    LAST_NAME VARCHAR2(25),
    EMAIL VARCHAR2(50),
    PHONE_NUMBER VARCHAR2(20),
    HIRE_DATE DATE,
    JOB_ID VARCHAR2(10),
    SALARY NUMBER(8,2),
    DEPARTMENT_ID NUMBER(4)
);
INSERT INTO EMPLOYEES VALUES
(100, 'Steven', 'King', 'SKING', '515.123.4567',
 TO_DATE('17-06-2003','DD-MM-YYYY'), 'AD_PRES', 24000, 90);

INSERT INTO EMPLOYEES VALUES
(101, 'Neena', 'Kochhar', 'NKOCHHAR', '515.123.4568',
 TO_DATE('21-09-2005','DD-MM-YYYY'), 'AD_VP', 17000, 90);

INSERT INTO EMPLOYEES VALUES
(102, 'Lex', 'De Haan', 'LDEHAAN', '515.123.4569',
 TO_DATE('13-01-2001','DD-MM-YYYY'), 'AD_VP', 17000, 90);

INSERT INTO EMPLOYEES VALUES
(103, 'Alexander', 'Hunold', 'AHUNOLD', '590.423.4567',
 TO_DATE('03-01-2006','DD-MM-YYYY'), 'IT_PROG', 9000, 60);

INSERT INTO EMPLOYEES VALUES
(104, 'Bruce', 'Ernst', 'BERNST', '590.423.4568',
 TO_DATE('21-05-2007','DD-MM-YYYY'), 'IT_PROG', 6000, 60);

COMMIT;

SELECT * FROM EMPLOYEES;

BEGIN

    UPDATE EMPLOYEES
    SET SALARY = SALARY * 2.12
    WHERE JOB_ID = 'AD_VP';

    IF SQL%ROWCOUNT > 0 THEN
        DBMS_OUTPUT.PUT_LINE(SQL%ROWCOUNT || ' ROWS UPDATED');
    ELSE
        DBMS_OUTPUT.PUT_LINE('NO ROWS UPDATED');
    END IF;

END;
/
